Runtime Library Optimizations - State
======================================

Branch: runtime-lib-optimizations
Plan: specs/runtime-library-optimizations.txt (general)

Phase 1: Lookup Tables + Arg Index
  1.1 alloc.c TheFlags() lookup table      — DONE
  1.2 say.c aud_chan_masks() lookup table   — DONE
  1.3 arg.c O(1) argument lookup           — DONE

Phase 2: Dead Code + Minor Fixes
  2.1 gadget.c dead commented-out code     — DONE
  2.2 sleep.c precompute FFP constant      — DONE

Phase 3: Library Open Caching
  3.1 DiskfontBase caching                 — REVERTED (cross-module deps pull unwanted .o)
  3.2 ILBMBase caching                     — REVERTED (same issue: alloc.c → iff.o → _LoadIFFToWindow)

Phase 4: Dynamic Allocation
  4.1 ser.c on-demand serial channels      — DONE
  4.2 gadget.c on-demand gadget allocation — DONE

=== VERIFIED ===
Build: cd src/make && make -f Makefile-lib clean all — OK (no errors)
Tests: cd ACE:verify/tests && rx runner.rexx all — 213 passed, 0 failed
