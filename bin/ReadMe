This directory contains the primary executables (and then some) for compiling 
ACE programs. The pre-processor acpp is a C-style preprocessor which
permits inclusion of more complex header files (see ACE:Include directory).
